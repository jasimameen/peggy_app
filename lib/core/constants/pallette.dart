import 'package:flutter/painting.dart';

/// color pallte used inside this app
class Pallete {
  const Pallete._();

  static const offWhite = Color.fromARGB(255, 233, 230, 230);
  static const green = Color.fromRGBO(5, 151, 106, 1);
  static const offGreen = Color.fromARGB(248, 82, 215, 175);
}

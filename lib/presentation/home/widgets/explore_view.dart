import 'package:flutter/material.dart';

class HomeExploreTabView extends StatelessWidget {
  const HomeExploreTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 500,
      child: const Center(child: Text('Explore view hehe')),
    );
  }
}
